{
  "name": "spacedrift-brand-kit",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Generator for the spacedrift.in brand and social-media asset kit.",
  "scripts": {
    "render": "NODE_PATH=$(npm root -g) PLAYWRIGHT_BROWSERS_PATH=${PLAYWRIGHT_BROWSERS_PATH:-/opt/pw-browsers} node render.mjs",
    "motion": "NODE_PATH=$(npm root -g) PLAYWRIGHT_BROWSERS_PATH=${PLAYWRIGHT_BROWSERS_PATH:-/opt/pw-browsers} node motion.mjs"
  },
  "dependencies": {
    "fontkit": "^2.0.4",
    "geist": "^1.7.2"
  },
  "optionalDependencies": {
    "@ffmpeg-installer/linux-x64": "^4.1.0"
  }
}
